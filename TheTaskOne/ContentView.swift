 

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("")
//     TaskOne(title: "Test", ditels: "new one", Laction: {}, Raction: {})
    }
}

#Preview {
    ContentView()
}
